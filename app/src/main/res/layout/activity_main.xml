<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF1C1C1E">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- 标题 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="灵动岛 ColorOS"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="4dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="高保真仿 iOS Dynamic Island"
            android:textSize="14sp"
            android:textColor="#99FFFFFF"
            android:layout_marginBottom="32dp" />

        <!-- 状态卡片 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_card"
            android:padding="20dp"
            android:layout_marginBottom="24dp">

            <TextView
                android:id="@+id/tv_status"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:textColor="#FFFFFF"
                android:lineSpacingExtra="4dp"
                android:text="正在检测权限状态..." />

        </LinearLayout>

        <!-- 权限按钮组 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="权限设置"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/btn_overlay"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="🔮 开启悬浮窗权限"
            android:textSize="15sp"
            android:backgroundTint="#FF0A84FF"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/btn_battery"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="🔋 加入电池白名单"
            android:textSize="15sp"
            android:backgroundTint="#FF34C759"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/btn_autostart"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="🚀 开启自启动管理"
            android:textSize="15sp"
            android:backgroundTint="#FFFF9500"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/btn_notification"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="🔔 开启通知监听"
            android:textSize="15sp"
            android:backgroundTint="#FF5E5CE6"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="24dp" />

        <!-- 启动按钮 -->
        <Button
            android:id="@+id/btn_start_service"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:text="⏳ 检查权限中..."
            android:textSize="18sp"
            android:textStyle="bold"
            android:backgroundTint="#FF30D158"
            android:textColor="#FFFFFF"
            android:enabled="false" />

        <!-- 底部提示 -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="提示：开启所有权限后点击启动按钮。锁屏后灵动岛依然会保持显示。ColorOS 用户请务必在「手机管家」中将本App加入电池白名单和自启动列表。"
            android:textSize="12sp"
            android:textColor="#66FFFFFF"
            android:lineSpacingExtra="2dp"
            android:layout_marginTop="16dp" />

    </LinearLayout>
</ScrollView>
